## 1. 真实运行时可行性验证

- [x] 1.1 基于真实 Minium 运行时补一个最小验证脚本，确认底层能够稳定发送 `touch_start`、`touch_move`、`touch_end` 和第二指插入点击
- [x] 1.2 用目标场景或最小复现页验证“两指以内复合交互”主链路，至少覆盖“第一指按住移动 + 第二指点击”
- [x] 1.3 记录真实运行时验证结论与已知限制，明确哪些组件或交互模式暂不保证完全兼容

## 2. 会话状态与领域模型扩展

- [x] 2.1 扩展会话模型与仓库，新增 `active_pointers`、`latest_gesture_event` 等手势上下文字段
- [x] 2.2 实现活跃触点状态维护逻辑，覆盖创建、更新、释放、超时清理和会话关闭清理
- [x] 2.3 为手势相关的会话状态补充结构化错误约束，覆盖无效 `pointer_id`、状态不一致和超过两指上限等场景

## 3. 运行时适配与手势服务

- [x] 3.1 在 Minium 适配层实现高层目标解析，支持结构化定位器与绝对页面坐标两种手势目标
- [x] 3.2 在 Minium 适配层实现基于活跃触点集合的 `touches` / `changedTouches` 组装逻辑
- [x] 3.3 实现 `touch_start`、`touch_move`、`touch_end`、`touch_tap` 的服务层封装，并统一返回活跃触点摘要
- [x] 3.4 明确并实现“两指以内”的能力边界，拒绝原始事件注入和超过边界的触点请求

## 4. MCP 工具与失败取证

- [x] 4.1 注册手势类 MCP 工具，至少覆盖 `miniapp_touch_start`、`miniapp_touch_move`、`miniapp_touch_end`、`miniapp_touch_tap`
- [x] 4.2 为手势工具补齐请求参数 schema 与返回 schema，统一包含 `pointer_id`、目标摘要和活跃触点摘要
- [x] 4.3 扩展失败取证逻辑，在截图之外补充事件类型、变更触点、活跃触点和目标解析结果等调试信息
- [x] 4.4 为手势成功与失败路径补齐中英文消息模板，保持结构化字段稳定

## 5. 测试与验收覆盖

- [x] 5.1 为占位运行时补充手势相关测试，覆盖单指按住、多步移动、第二指点击和触点释放
- [x] 5.2 为真实运行时补充至少一条可执行的集成验证路径，确认会话状态与底层手势执行保持一致
- [x] 5.3 增加失败路径测试，覆盖目标解析失败、无效 `pointer_id`、超过两指上限和手势取证输出
- [x] 5.4 补充文档说明，记录手势工具的使用方式、本期边界以及 Minium 真实运行时的已知限制
