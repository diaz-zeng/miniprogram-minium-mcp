## 1. 项目骨架与运行配置

- [x] 1.1 建立 Python MCP Server 的基础目录结构，至少补齐 `src/server`、`src/domain`、`src/adapters/minium`、`src/support`
- [x] 1.2 初始化项目依赖与启动入口，确保服务可以通过本地 `stdio` 方式启动
- [x] 1.3 增加本地配置加载能力，支持通过环境变量或配置文件注入 Minium、微信开发者工具和产物目录参数
- [x] 1.4 实现基础日志与 `artifacts/` 目录管理，统一截图和调试产物的落盘规则
- [x] 1.5 增加环境语言检测与语言配置能力，支持中文环境输出中文、非中文环境输出英文

## 2. 会话管理能力

- [x] 2.1 实现 Minium 会话仓库，支持创建、查询、更新和关闭会话
- [x] 2.2 实现会话创建流程，覆盖“提供项目路径 -> 服务自动执行 `auto + attach` -> 返回唯一 `session_id`”主链路
- [x] 2.3 实现会话超时与清理机制，确保失活会话会自动释放资源
- [x] 2.4 实现当前页面读取与手动截图能力，并把结果纳入会话上下文摘要
- [x] 2.5 为会话相关工具补齐结构化错误返回，覆盖环境缺失、连接失败和无效会话场景

## 3. 页面动作与断言能力

- [x] 3.1 实现结构化定位器模型及其参数校验，首期支持约定的基础定位策略
- [x] 3.2 实现元素查询能力，返回可序列化的元素摘要而不是底层句柄
- [x] 3.3 实现点击与输入动作，统一处理元素不存在、不可见和不可交互场景
- [x] 3.4 实现显式等待能力，覆盖首期支持的页面或元素条件，并支持超时配置
- [x] 3.5 实现页面路径、元素文本与元素存在性或可见性的结构化断言能力

## 4. 错误模型与取证能力

- [x] 4.1 定义统一错误码与返回结构，覆盖 `ENVIRONMENT_ERROR`、`SESSION_ERROR`、`ACTION_ERROR`、`ASSERTION_FAILED` 和 `INTERNAL_ERROR`
- [x] 4.2 在动作失败、等待超时和断言失败时自动触发一次基础取证
- [x] 4.3 将截图路径、页面路径、定位器摘要和超时信息写入结构化 `details` 或 `artifacts` 返回字段
- [x] 4.4 明确并实现“仅限验收用途”的能力边界，拒绝任意脚本执行或未受控底层操作
- [x] 4.5 为会话、动作、断言和 CLI 帮助补齐统一的中英文消息模板层

## 5. MCP 工具对外暴露

- [x] 5.1 注册会话类 MCP 工具，至少覆盖创建会话、关闭会话、获取当前页面和截图
- [x] 5.2 注册动作类 MCP 工具，至少覆盖查询元素、点击、输入和等待
- [x] 5.3 注册断言类 MCP 工具，至少覆盖页面路径、元素文本和可见性或存在性断言
- [x] 5.4 为所有 MCP 工具补齐统一的请求参数 schema 与返回 schema

## 6. 集成验证与文档

- [x] 6.1 增加最小集成验证，覆盖“创建会话 -> 获取当前页面 -> 查询元素 -> 截图 -> 断言”主链路
- [x] 6.2 增加至少一个失败路径验证，确认动作失败或断言失败时会返回结构化错误和证据产物
- [x] 6.3 在 README 或新增文档中补充本地运行说明，明确这是本地 `stdio` 型 MCP Server 的接入方式
- [x] 6.4 补充开发说明，记录本地依赖前提、常见环境问题与排查方式
